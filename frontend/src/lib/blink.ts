// Blink SDK has been removed. This file is kept as a stub to prevent import errors.
// Auth is now handled via useAuth hook with custom signup/signin + Google OAuth.
export const blink = {
  auth: {
    getUser: () => null,
    signIn: () => {},
    signOut: () => {},
  },
}
